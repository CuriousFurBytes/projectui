[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"
  VITE_BASE = "/"

# SPA fallback (single index.html)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
